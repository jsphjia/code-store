#use <conio>

int main () {

  // A stack of strings
  stack_t S = stack_new();

  string s1 = "hello";        // Push a value
  push(S, s1);

  push(S, "world");           // Push another

  string s2 = pop(S);         // Pop it
  printf("%s\n", s2);

  printf("%s\n", pop(S));            // Pop the first one



  return 0;
}
